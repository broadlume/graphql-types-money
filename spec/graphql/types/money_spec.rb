# frozen_string_literal: true

RSpec.describe GraphQL::Types::Money do
  it 'has a version number' do
    expect(GraphQL::Types::Money::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
